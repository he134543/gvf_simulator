# gvf_simulator
A simple program to compute water surface elevation profiles for gradually varied flows.

# How to run it
Clone the repository and follow the step of `run_and_view_gvf.ipynb` to run it. Or just open this repo in github codespace, install `numpy`, `pandas`, `matplotlib`, `seaborn`, `holoviews`, and run the notebook.